kind: pipeline
name: default

steps:
  - name: replication_init
    image: mongo:latest
    commands:
      - mongo --host m1 .ci/rs.js

services:
  - name: m1
    image: mongo:latest
  - name: m2
    image: mongo:latest
  - name: m3
    image: mongo:latest
